<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The page defaults to dark and carries its own light mode, so the shell
         looks identical either way; the entry exists so the night qualifier does
         not fall back to a different parent. -->
    <style name="Theme.SolarCast" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/brand_background</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
