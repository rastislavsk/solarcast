<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        The page's default background (terracotta scheme, dark). The window and
        the splash are painted with it so nothing flashes a foreign colour
        before the WebView draws its first frame; from then on the page reports
        its actual background and the shell follows.
    -->
    <color name="brand_background">#100E17</color>
</resources>
