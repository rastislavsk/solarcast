# Names the locale that the unqualified res/values folder is written in, so AGP
# can generate res/xml/_generated_res_locale_config.xml from the res-* folders.
# That file is what puts SolarCast in Settings > System > Languages > App languages.
unqualifiedResLocale=en-US
